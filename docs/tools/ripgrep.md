# ripgrep

`ripgrep` is a fast text search tool. The command is `rg`.

## Common Commands

- `rg foo`: search for `foo`
- `rg -n foo src`: search with line numbers in `src`
- `rg -t ts useEffect`: search only TypeScript files
- `rg -g '*.md' TODO`: search only matching file globs
