# eslint_d

`eslint_d` is a daemonized version of ESLint for faster repeated runs.

## Common Commands

- `eslint_d .`: lint the current repo
- `eslint_d src/app.ts`: lint one file
- `eslint_d --fix .`: lint and auto-fix where possible
- `eslint_d restart`: restart the background daemon
