# coreutils

`coreutils` provides GNU versions of common Unix commands. On macOS, this is
mainly useful for tools like `gls`.

## Common Commands

- `gls`: GNU `ls`
- `gcp file1 file2`: GNU `cp`
- `gdate`: GNU `date`
- `gtimeout 5 command`: GNU `timeout`
