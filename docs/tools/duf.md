# duf

`duf` shows disk usage for mounted filesystems in a clean table.

## Common Commands

- `duf`: show mounted filesystem usage
- `duf / /tmp`: show usage for selected mount points
- `duf --json`: print machine-readable output
