# xh

`xh` is a friendly HTTP client similar to HTTPie.

## Common Commands

- `xh GET https://example.com`: make a GET request
- `xh POST https://example.com name=keegan`: send JSON-like fields
- `xh :3000/health`: use shorthand for a local URL
- `xh https://example.com Authorization:'Bearer token'`: add headers
