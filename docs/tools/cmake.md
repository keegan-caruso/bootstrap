# cmake

`cmake` configures and builds C and C++ projects, and some editor modules use
it for native compilation steps.

## Common Commands

- `cmake -S . -B build`: configure into a build directory
- `cmake --build build`: build the configured project
- `cmake --install build`: install built artifacts
- `cmake --version`: show the installed version
