# oxlint

`oxlint` is a high-performance JavaScript and TypeScript linter.

Common commands:

- `oxlint`: lint the current repository
- `oxlint src`: lint a directory
- `oxlint --fix`: apply safe fixes
