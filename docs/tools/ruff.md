# ruff

`ruff` is a fast Python linter and formatter.

## Common Commands

- `ruff check .`: lint the current repo
- `ruff check --fix .`: lint and auto-fix
- `ruff format .`: format Python files
- `ruff format file.py`: format one file
