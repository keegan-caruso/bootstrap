# procs

`procs` is a modern replacement for `ps`.

## Common Commands

- `procs`: list running processes
- `procs zsh`: filter by process name
- `procs --tree`: show process hierarchy
- `procs --sortd cpu`: sort descending by CPU usage
