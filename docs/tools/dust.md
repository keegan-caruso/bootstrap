# dust

`dust` is a modern alternative to `du` for directory size summaries.

## Common Commands

- `dust`: show directory sizes in the current location
- `dust -d 2`: limit output depth
- `dust src`: inspect one path
