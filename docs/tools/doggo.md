# doggo

`doggo` is a DNS lookup tool.

## Common Commands

- `doggo example.com`: resolve with the default record type
- `doggo A example.com`: query A records
- `doggo TXT example.com`: query TXT records
- `doggo @1.1.1.1 example.com`: query a specific resolver
