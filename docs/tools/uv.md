# uv

`uv` is a fast Python package and environment manager.

## Common Commands

- `uv init`: initialize a Python project
- `uv sync`: sync the project environment
- `uv add requests`: add a dependency
- `uv run python`: run Python in the managed environment
