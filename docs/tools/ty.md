# ty

`ty` provides Python type checking and a language server.

## Common Commands

- `ty check`: type-check the current project
- `ty check path/to/file.py`: type-check one file
- `ty server`: start the language server
