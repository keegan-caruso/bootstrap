# fzf

`fzf` is a fuzzy finder for filtering long lists interactively.

## Common Commands

- `fzf`: fuzzy-pick lines from stdin
- `history | fzf`: search shell history
- `fd . | fzf`: fuzzy-pick a file from the current repo
- `git branch | fzf`: fuzzy-pick a branch from a list
