# fd

`fd` is a fast, friendly file finder.

## Common Commands

- `fd foo`: find files or directories matching `foo`
- `fd -t f foo`: find only files
- `fd -t d src`: find only directories
- `fd -e ts`: find files by extension
