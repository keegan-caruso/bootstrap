# yamllint

`yamllint` checks YAML files for syntax and style problems.

## Common Commands

- `yamllint file.yml`: lint one file
- `yamllint .`: lint YAML in the current directory
- `yamllint -d relaxed file.yml`: use a looser built-in ruleset
