# bat

`bat` is a `cat` replacement with syntax highlighting and paging.

## Common Commands

- `bat file.ts`: view a file with syntax highlighting
- `bat -n file.sh`: show line numbers
- `bat --style=plain file.txt`: disable extra decorations
- `rg foo | bat`: view piped output with paging
