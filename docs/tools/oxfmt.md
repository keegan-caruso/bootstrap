# oxfmt

`oxfmt` is a high-performance formatter for JavaScript and TypeScript.

Common commands:

- `oxfmt .`: format supported files in a repository
- `oxfmt --check .`: check formatting without writing
- `oxfmt file.ts`: format one file
