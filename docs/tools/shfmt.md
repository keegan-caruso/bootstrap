# shfmt

`shfmt` formats shell scripts consistently.

## Common Commands

- `shfmt -w script.sh`: format one file in place
- `shfmt -w .`: format shell files in the current tree
- `shfmt -d script.sh`: show a diff instead of writing
