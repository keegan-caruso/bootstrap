# cmark

`cmark` is a CommonMark Markdown parser and renderer.

## Common Commands

- `cmark README.md`: render Markdown to HTML
- `cmark --to commonmark README.md`: normalize Markdown
- `cmark --validate-utf8 README.md`: validate input while rendering
