# shellcheck

`shellcheck` statically analyzes shell scripts for mistakes.

## Common Commands

- `shellcheck script.sh`: lint one shell script
- `shellcheck *.sh`: lint multiple scripts
- `shellcheck -x script.sh`: follow sourced files when possible
