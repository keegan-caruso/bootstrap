# prettier

`prettier` formats code and markup with consistent style rules.

## Common Commands

- `prettier --write .`: format supported files in a repo
- `prettier --check .`: check formatting without writing
- `prettier file.ts --write`: format one file
- `prettier package.json`: print formatted output to stdout
