# taplo

`taplo` formats, validates, and serves TOML through LSP.

## Common Commands

- `taplo fmt file.toml`: format one TOML file
- `taplo fmt .`: format TOML files in a repo
- `taplo lint file.toml`: lint one TOML file
- `taplo lsp stdio`: start the language server
