#!/usr/bin/env bash
set -euo pipefail

if ! command -v git.exe >/dev/null 2>&1; then
  printf 'git-credential-manager-wsl: Git for Windows is not available on PATH\n' >&2
  exit 1
fi

exec git.exe credential-manager "$@"
