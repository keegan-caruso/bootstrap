#!/usr/bin/env bash
set -euo pipefail

git_exe="/mnt/c/Program Files/Git/cmd/git.exe"

if [[ ! -x "$git_exe" ]]; then
  printf 'git-credential-manager-wsl: Git for Windows is not installed at %s\n' "$git_exe" >&2
  exit 1
fi

exec "$git_exe" credential-manager "$@"
